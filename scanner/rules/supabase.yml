rules:
  - id: supabase-service-role-client-usage
    pattern: "createClient(.*SERVICE_ROLE_KEY.*)"
    message: "Potential usage of Supabase Service Role Key on the client side. This bypasses RLS."
    severity: "CRITICAL"
