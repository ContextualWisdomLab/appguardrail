### Added

- Added fail-closed, exact-head comparison of live GitHub Code Scanning tool, category, and matrix identities between a pull request's base branch and current analyses.
- Added bounded IssueOps publication for confirmed drift while preserving permission, service, pagination, malformed-payload, and inexact-head states as explicit `unknown` telemetry without false findings.
- Added least-privilege organization collection using distinct repository-read and target-only issue-write GitHub App tokens.
