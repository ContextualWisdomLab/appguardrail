Changed
- Dashboard findings upload now uses the existing primary action button while preserving the native file picker as the browser boundary. The selected filename continues to be reported after a valid findings file is loaded.

Accessibility
- The visible upload control is a native button with its visible text as the accessible name; the backing file input is hidden to avoid duplicate controls.
