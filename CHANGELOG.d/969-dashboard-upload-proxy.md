### Changed

- Replaced browser-dependent native file-input chrome with a tokenized, keyboard-native `Upload file` proxy while preserving drag-and-drop and same-file reselection.
- Removed the hidden input's duplicate accessible name and added reusable hover, focus, and disabled visual-state contracts without claiming whole-product accessibility conformance.
