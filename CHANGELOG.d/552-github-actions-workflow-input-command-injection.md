## Added

- Add the CRITICAL `github-actions-workflow-input-command-injection` detector for caller-controlled `workflow_call` or `workflow_dispatch` string inputs interpolated directly into GitHub Actions `run` programs.
- Preserve exact vulnerable and reviewed fixed central-workflow Git objects, name-bound positive and negative oracles, production scanner evidence, quoted or whitespace-varied YAML grammar, and blank-line regression coverage.
- Bound the YAML line matcher and add a large near-miss regression test to guard against excessive backtracking during repository scans.
- Document the environment-variable remediation boundary, declared limitations, CWE-78/CWE-94 mapping, OWASP A03:2021 mapping, and APA 7 source traceability.
