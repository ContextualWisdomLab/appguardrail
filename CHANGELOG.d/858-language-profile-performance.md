### Changed

- Reduced zero-configuration language-profile path allocation overhead for large repositories while preserving string and `Path` inputs, one-shot iterables, cross-platform separators, manifest detection, and web-directory signals.
