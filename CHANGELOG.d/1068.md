```markdown
# AppGuardrail v1.0.0

## [Security]
- **HIGH**: Fix empty hostname Server-Side Request Forgery (SSRF) bypass in URL validator. The validator now strictly rejects empty hostnames before DNS resolution and properly fails-closed on `socket.gaierror`.
```
